# SoM-MIMO WiFo

Local SoM-MIMO/WiFo experiment code baseline before semantic CSI gate changes.

Large artifacts such as datasets, model weights, outputs, and generated experiment assets are intentionally not committed.
