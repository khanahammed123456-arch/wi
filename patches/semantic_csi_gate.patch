# OBSOLETE PATCH

The semantic CSI gate patch was reverted after testing because it reduced AP in the tested setting.

Current active implementation should use direct WiFo residual CSI compensation:

```python
h_pred_som = h_outdated + delta_h
```

Do not apply this patch for the current main experiment.
